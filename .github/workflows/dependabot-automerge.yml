name: Dependabot Auto-Merge

on:
  schedule:
    - cron: "30 5 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: basel5001/githubinfra/.github/workflows/dependabot-automerge.yml@main
